name: anchieta_flutter_todo
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: ^10.2.0
  http: ^1.6.0
  jwt_decoder: ^2.0.1
  path: ^1.9.1
  shared_preferences: ^2.5.5
  sqflite: ^2.4.2
  uuid: ^4.5.3
  workmanager: ^0.9.0+3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
